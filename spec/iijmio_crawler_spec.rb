require 'spec_helper'

describe IijmioCrawler do
  it 'has a version number' do
    expect(IijmioCrawler::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
